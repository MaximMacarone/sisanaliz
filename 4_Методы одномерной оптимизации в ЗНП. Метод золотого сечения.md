Идея этого метода состоит в использовании на каждой новой итерации для сокращения интервала неопределённости одной из внутренних точек предыдущей итерации. Это может быть сделано при выполнении следующих условий:
![[Screenshot 2026-05-29 at 17.50.23.png]]
**_Алгоритм метода золотого сечения:_**
![[Screenshot 2026-05-29 at 17.55.33.png]]

----
1) выбрать l > 0, [a1, b1], 
2) положить lambda1 = a1 + (1-alpha) * (b1-a1), mu1 = a1 + alpha * (b1-a1), alpha = 0,618
3) вычислить f(lambda1), f(mu1)
4) Если 